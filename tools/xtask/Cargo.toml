[package]
name = "virtio-mem-xtask"
version = "0.1.0"
edition = "2021"
authors = ["macg4dave"]
license = "MIT"
description = "Repository build, test, and validation control-plane tooling"
publish = false

[dependencies]
serde_json = "1.0"
signal-hook = "0.3"
virtio-mem-core = { path = "../../crates/virtio-mem-core" }
wait-timeout = "0.2"
